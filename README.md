# PPAbenchStatic

Static leaderboard website for PPAbench, generated from the local PPAbench and SoCmate repositories.

Open `index.html` directly or serve the folder with:

```bash
python3 -m http.server 4187
```

Leaderboard data lives in `data/leaderboard.json`.
